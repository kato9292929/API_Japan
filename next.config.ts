import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Enable static file serving from public/
  // .well-known/x402.json is served from public/.well-known/x402.json
};

export default nextConfig;
